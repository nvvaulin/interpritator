#include "interpretator.cpp"
#include "signature.h"
#include "collection.h"

class formula
{
	struct name_of_type
	{
		string s;
		char n;
	};
	vector<name_of_type> types;
	collection collect;
	signature sign;
	formula(char* s);
};

formula::furmula(char* s);
{
};
	